spring:
  application:
    name: checklist-web

  datasource:
    url: jdbc:postgresql:///defaultdb?sslmode=require
    username: avnadmin
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080